import "./chunk-QZ7TP4HQ.mjs";

// src/normalize.ts
function normalizeKey(eventKey) {
  if (eventKey.length === 1 && SINGLE_LETTER_REGEX.test(eventKey)) {
    return eventKey.toUpperCase();
  }
  const lower = eventKey.toLowerCase();
  const normalized = KEY_NORMALIZATION_MAP.get(lower);
  if (normalized !== void 0) {
    return normalized;
  }
  if (FUNCTION_KEY_REGEX.test(lower)) {
    return lower.toUpperCase();
  }
  return eventKey;
}
var SINGLE_LETTER_REGEX = /[a-z]/i;
var FUNCTION_KEY_REGEX = /^f([1-9]|1[0-9])$/;
var KEY_NORMALIZATION_MAP = /* @__PURE__ */ new Map([
  // Special keys
  ["backspace", "Backspace"],
  ["tab", "Tab"],
  ["clear", "Clear"],
  ["enter", "Enter"],
  ["return", "Enter"],
  ["esc", "Escape"],
  ["escape", "Escape"],
  ["space", " "],
  ["spacebar", " "],
  // Arrow keys
  ["up", "ArrowUp"],
  ["arrowup", "ArrowUp"],
  ["down", "ArrowDown"],
  ["arrowdown", "ArrowDown"],
  ["left", "ArrowLeft"],
  ["arrowleft", "ArrowLeft"],
  ["right", "ArrowRight"],
  ["arrowright", "ArrowRight"],
  // Page navigation
  ["pageup", "PageUp"],
  ["pgup", "PageUp"],
  ["pagedown", "PageDown"],
  ["pgdn", "PageDown"],
  ["home", "Home"],
  ["end", "End"],
  // Delete/Insert keys
  ["del", "Delete"],
  ["delete", "Delete"],
  ["ins", "Insert"],
  ["insert", "Insert"]
]);
export {
  normalizeKey
};
