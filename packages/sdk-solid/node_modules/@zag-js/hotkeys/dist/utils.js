"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/utils.ts
var utils_exports = {};
__export(utils_exports, {
  MODIFIER_SEPARATOR: () => MODIFIER_SEPARATOR,
  SEQUENCE_SEPARATOR: () => SEQUENCE_SEPARATOR,
  getEventTarget: () => getEventTarget,
  getPlatform: () => getPlatform,
  getWin: () => getWin,
  isContentEditableElement: () => isContentEditableElement,
  isEventWithinTarget: () => isEventWithinTarget,
  isFormTag: () => isFormTag,
  isSymbolKey: () => isSymbolKey,
  resolveCommandTarget: () => resolveCommandTarget,
  toArray: () => toArray
});
module.exports = __toCommonJS(utils_exports);
var import_dom_query = require("@zag-js/dom-query");
var getWin = (root) => (0, import_dom_query.getWindow)(root);
var FORM_TAGS = /* @__PURE__ */ new Set(["input", "textarea", "select"]);
var isFormTag = (tagName) => FORM_TAGS.has(tagName);
var MODIFIER_SEPARATOR = "+";
var SEQUENCE_SEPARATOR = ">";
var getEventTarget = (event) => {
  const target = (0, import_dom_query.getEventTarget)(event);
  return (0, import_dom_query.isHTMLElement)(target) ? target : null;
};
var isContentEditableElement = (target) => {
  return (0, import_dom_query.isHTMLElement)(target) && target.isContentEditable;
};
var getPlatform = () => {
  if (typeof navigator === "undefined") return "windows";
  if ((0, import_dom_query.isApple)()) return "mac";
  if ((0, import_dom_query.isLinux)()) return "linux";
  return "windows";
};
var toArray = (value) => {
  if (Array.isArray(value)) {
    return value.filter((item) => item !== void 0);
  }
  return value !== void 0 ? [value] : [];
};
var resolveCommandTarget = (target) => {
  return typeof target === "function" ? target() : target;
};
var isEventWithinTarget = (event, el) => {
  const path = event.composedPath?.();
  if (path) return path.includes(el);
  const target = getEventTarget(event);
  return target !== null && (el === target || (0, import_dom_query.contains)(el, target));
};
var UNICODE_LETTER = /^\p{L}$/u;
var UNICODE_NUMBER = /^\p{N}$/u;
var isSymbolKey = (key) => {
  if (key.length !== 1) return false;
  if (UNICODE_LETTER.test(key) || UNICODE_NUMBER.test(key)) return false;
  return true;
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  MODIFIER_SEPARATOR,
  SEQUENCE_SEPARATOR,
  getEventTarget,
  getPlatform,
  getWin,
  isContentEditableElement,
  isEventWithinTarget,
  isFormTag,
  isSymbolKey,
  resolveCommandTarget,
  toArray
});
