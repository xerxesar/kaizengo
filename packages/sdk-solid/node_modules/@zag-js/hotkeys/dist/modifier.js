"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/modifier.ts
var modifier_exports = {};
__export(modifier_exports, {
  isModKey: () => isModKey,
  isModifierSet: () => isModifierSet,
  normalizeModifier: () => normalizeModifier,
  resolveControlOrMeta: () => resolveControlOrMeta
});
module.exports = __toCommonJS(modifier_exports);
var MODIFIER_NORMALIZATION_MAP = /* @__PURE__ */ new Map([
  ["ctrl", "Control"],
  ["control", "Control"],
  ["alt", "Alt"],
  ["option", "Alt"],
  ["shift", "Shift"],
  ["meta", "Meta"],
  ["cmd", "Meta"],
  ["command", "Meta"],
  ["win", "Meta"],
  ["windows", "Meta"]
]);
var normalizeModifier = (key) => MODIFIER_NORMALIZATION_MAP.get(key.toLowerCase()) ?? key;
var MODIFIER_SET = /* @__PURE__ */ new Set(["control", "ctrl", "alt", "option", "shift", "meta", "cmd", "command", "win", "windows"]);
var isModifierSet = (key) => MODIFIER_SET.has(key.toLowerCase());
function resolveControlOrMeta(key, platform) {
  return isModKey(key.toLowerCase()) ? platform === "mac" ? "Meta" : "Control" : key;
}
function isModKey(key) {
  return key.toLowerCase() === "mod" || key.toLowerCase() === "controlormeta";
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  isModKey,
  isModifierSet,
  normalizeModifier,
  resolveControlOrMeta
});
