import { Platform } from './types.js';

declare const normalizeModifier: (key: string) => string;
declare const isModifierSet: (key: string) => boolean;
declare function resolveControlOrMeta(key: string, platform: Platform): string;
declare function isModKey(key: string): boolean;

export { isModKey, isModifierSet, normalizeModifier, resolveControlOrMeta };
