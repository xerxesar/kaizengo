import { HotkeyFormatOptions, HotkeyStore, CommandDefinition, HotkeyRecorderOptions, HotkeyRecorderState, HotkeyCommand } from '@zag-js/hotkeys';
export { CommandDefinition, ConflictBehavior, HotkeyAction, HotkeyCommand, HotkeyFormatOptions, HotkeyOptions, HotkeyStore, HotkeyStoreOptions, ParsedHotkey, Platform, RecordedHotkey, createHotkeyStore, formatHotkey, isHotKey, isHotkeyEqual, parseHotkey, validateHotkey } from '@zag-js/hotkeys';
import { M as MaybeAccessor } from '../../types-Sr_80hg9.js';
import { Accessor } from 'solid-js';

type UseFormatHotkeyReturn = (hotkey: string, options?: HotkeyFormatOptions) => string;
declare const useFormatHotkey: () => UseFormatHotkeyReturn;

interface UseHotkeyStoreProps {
    /**
     * The store to read from and register on. Defaults to a store shared by every hook that
     * does not name one.
     *
     * Keep it stable: build it outside the component, since a store created during render is
     * rebuilt on every read and loses its registrations.
     */
    store?: HotkeyStore | undefined;
}
declare const useHotkeyStore: (props?: UseHotkeyStoreProps) => HotkeyStore;

interface UseHotkeysCommand extends Omit<CommandDefinition, 'id'> {
    /**
     * Identifies the command across renders. One is generated when omitted, which is enough
     * unless something else needs to address this command by name.
     */
    id?: string | undefined;
}
interface UseHotkeysProps extends UseHotkeyStoreProps {
    /**
     * The commands to register.
     */
    commands: UseHotkeysCommand[];
    /**
     * Prefix for the ids generated for commands that do not set their own. One is generated
     * when omitted.
     */
    id?: string | undefined;
}
declare const useHotkeys: (props: MaybeAccessor<UseHotkeysProps>) => void;

interface UseHotkeyProps extends UseHotkeysCommand, Pick<UseHotkeysProps, 'store'> {
}
declare const useHotkey: (props: MaybeAccessor<UseHotkeyProps>) => void;

interface UseHotkeyRecorderProps extends Omit<HotkeyRecorderOptions, 'target'> {
}
interface UseHotkeyRecorderReturn {
    /**
     * The current recorder state.
     */
    state: Accessor<HotkeyRecorderState>;
    /**
     * Start listening for key events.
     */
    start: () => void;
    /**
     * Stop listening and finalize the recorded hotkey.
     */
    stop: () => void;
    /**
     * Stop listening and discard the recorded hotkey.
     */
    cancel: () => void;
    /**
     * Clear the recorded hotkey.
     */
    clear: () => void;
}
declare const useHotkeyRecorder: (props?: MaybeAccessor<UseHotkeyRecorderProps>) => UseHotkeyRecorderReturn;

declare const useHotkeyRegistrations: (props?: UseHotkeyStoreProps) => Accessor<HotkeyCommand[]>;

interface UseIsKeyPressedProps extends UseHotkeyStoreProps {
    /**
     * The key or combination to watch.
     */
    hotkey: string;
}
declare const useIsKeyPressed: (props: MaybeAccessor<UseIsKeyPressedProps>) => Accessor<boolean>;

type Platform = 'mac' | 'windows' | 'linux';
declare const usePlatform: () => Accessor<Platform>;

declare const usePressedKeys: (props?: UseHotkeyStoreProps) => Accessor<string[]>;

export { type UseFormatHotkeyReturn, type UseHotkeyProps, type UseHotkeyRecorderProps, type UseHotkeyRecorderReturn, type UseHotkeyStoreProps, type UseHotkeysCommand, type UseHotkeysProps, type UseIsKeyPressedProps, useFormatHotkey, useHotkey, useHotkeyRecorder, useHotkeyRegistrations, useHotkeyStore, useHotkeys, useIsKeyPressed, usePlatform, usePressedKeys };
