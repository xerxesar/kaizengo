import {
  createSplitProps
} from "./6WEDGJKQ.jsx";
import {
  useEnvironmentContext
} from "./E2PCA7FB.jsx";
import {
  useLocaleContext
} from "./HOPBYFHD.jsx";
import {
  ark
} from "./UFYZ7HLU.jsx";
import {
  createContext
} from "./UZJJWJQM.jsx";
import {
  __export
} from "./7IUG3E2V.jsx";

// src/components/toc/toc-content.tsx
var TocContent = (props) => {
  return <ark.article {...props} />;
};

// src/components/toc/use-toc-context.ts
var [TocProvider, useTocContext] = createContext({
  hookName: "useTocContext",
  providerName: "<TocProvider />"
});

// src/components/toc/toc-context.tsx
var TocContext = (props) => props.children(useTocContext());

// src/components/toc/toc-indicator.tsx
import { mergeProps } from "@zag-js/solid";
var TocIndicator = (props) => {
  const toc2 = useTocContext();
  const mergedProps = mergeProps(() => toc2().getIndicatorProps(), props);
  return <ark.div {...mergedProps} />;
};

// src/components/toc/toc-item.tsx
import { mergeProps as mergeProps2 } from "@zag-js/solid";

// src/components/toc/use-toc-item-props-context.ts
var [TocItemPropsProvider, useTocItemPropsContext] = createContext({
  hookName: "useTocItemPropsContext",
  providerName: "<TocItemPropsProvider />"
});

// src/components/toc/toc-item.tsx
var splitItemProps = createSplitProps();
var TocItem = (props) => {
  const [itemProps, localProps] = splitItemProps(props, ["item"]);
  const toc2 = useTocContext();
  const mergedProps = mergeProps2(() => toc2().getItemProps(itemProps), localProps);
  return <TocItemPropsProvider value={itemProps}>
      <ark.li {...mergedProps} />
    </TocItemPropsProvider>;
};

// src/components/toc/toc-link.tsx
import { mergeProps as mergeProps3 } from "@zag-js/solid";
var TocLink = (props) => {
  const toc2 = useTocContext();
  const itemProps = useTocItemPropsContext();
  const mergedProps = mergeProps3(() => toc2().getLinkProps(itemProps), props);
  return <ark.a {...mergedProps} />;
};

// src/components/toc/toc-list.tsx
import { mergeProps as mergeProps4 } from "@zag-js/solid";
var TocList = (props) => {
  const toc2 = useTocContext();
  const mergedProps = mergeProps4(() => toc2().getListProps(), props);
  return <ark.ul {...mergedProps} />;
};

// src/components/toc/toc-nav.tsx
import { mergeProps as mergeProps5 } from "@zag-js/solid";
var TocNav = (props) => {
  const { placement, ...rest } = props;
  const toc2 = useTocContext();
  const mergedProps = mergeProps5(() => toc2().getRootProps(), rest);
  return <ark.nav {...mergedProps} data-placement={placement} />;
};

// src/components/toc/toc-root.tsx
import { mergeProps as mergeProps6 } from "@zag-js/solid";

// src/components/toc/use-toc.ts
import { normalizeProps, useMachine } from "@zag-js/solid";
import * as toc from "@zag-js/toc";
import { createMemo, createUniqueId } from "solid-js";
var useToc = (props) => {
  const id = createUniqueId();
  const locale = useLocaleContext();
  const environment = useEnvironmentContext();
  const machineProps = createMemo(
    () => ({
      id: props?.id ?? id,
      dir: locale().dir,
      getRootNode: environment().getRootNode,
      items: props?.items ?? [],
      ...props
    })
  );
  const service = useMachine(toc.machine, machineProps);
  return createMemo(() => toc.connect(service, normalizeProps));
};

// src/components/toc/toc-root.tsx
var splitRootProps = createSplitProps();
var TocRoot = (props) => {
  const [useTocProps, localProps] = splitRootProps(props, [
    "activeIds",
    "autoScroll",
    "defaultActiveIds",
    "scrollEl",
    "id",
    "ids",
    "items",
    "onActiveChange",
    "rootMargin",
    "scrollBehavior",
    "threshold"
  ]);
  const toc2 = useToc(useTocProps);
  const mergedProps = mergeProps6(() => toc2().getRootProps(), localProps);
  return <TocProvider value={toc2}>
      <ark.div {...mergedProps} />
    </TocProvider>;
};

// src/components/toc/toc-root-provider.tsx
import { mergeProps as mergeProps7 } from "@zag-js/solid";
var TocRootProvider = (props) => {
  const [{ value: toc2 }, localProps] = createSplitProps()(props, ["value"]);
  const mergedProps = mergeProps7(() => toc2().getRootProps(), localProps);
  return <TocProvider value={toc2}>
      <ark.div {...mergedProps} />
    </TocProvider>;
};

// src/components/toc/toc-title.tsx
import { mergeProps as mergeProps8 } from "@zag-js/solid";
var TocTitle = (props) => {
  const toc2 = useTocContext();
  const mergedProps = mergeProps8(() => toc2().getTitleProps(), props);
  return <ark.h2 {...mergedProps} />;
};

// src/components/toc/toc.ts
var toc_exports = {};
__export(toc_exports, {
  Content: () => TocContent,
  Context: () => TocContext,
  Indicator: () => TocIndicator,
  Item: () => TocItem,
  Link: () => TocLink,
  List: () => TocList,
  Nav: () => TocNav,
  Root: () => TocRoot,
  RootProvider: () => TocRootProvider,
  Title: () => TocTitle
});

export {
  TocContent,
  useTocContext,
  TocContext,
  TocIndicator,
  TocItem,
  TocLink,
  TocList,
  TocNav,
  useToc,
  TocRoot,
  TocRootProvider,
  TocTitle,
  toc_exports
};
