import { createSplitProps } from './ZMHI4GDJ.js';
import { useEnvironmentContext } from './YVDVJI2W.js';
import { useLocaleContext } from './EM5SH6A3.js';
import { ark } from './EPLBB4QN.js';
import { createContext } from './THN5C4U6.js';
import { __export } from './ESLJRKWD.js';
import { createComponent, mergeProps as mergeProps$1 } from 'solid-js/web';
import { mergeProps, useMachine, normalizeProps } from '@zag-js/solid';
import * as toc from '@zag-js/toc';
import { createUniqueId, createMemo } from 'solid-js';

var TocContent = (props) => {
  return createComponent(ark.article, props);
};

// src/components/toc/use-toc-context.ts
var [TocProvider, useTocContext] = createContext({
  hookName: "useTocContext",
  providerName: "<TocProvider />"
});

// src/components/toc/toc-context.tsx
var TocContext = (props) => props.children(useTocContext());
var TocIndicator = (props) => {
  const toc2 = useTocContext();
  const mergedProps = mergeProps(() => toc2().getIndicatorProps(), props);
  return createComponent(ark.div, mergedProps);
};

// src/components/toc/use-toc-item-props-context.ts
var [TocItemPropsProvider, useTocItemPropsContext] = createContext({
  hookName: "useTocItemPropsContext",
  providerName: "<TocItemPropsProvider />"
});

// src/components/toc/toc-item.tsx
var splitItemProps = createSplitProps();
var TocItem = (props) => {
  const [itemProps, localProps] = splitItemProps(props, ["item"]);
  const toc2 = useTocContext();
  const mergedProps = mergeProps(() => toc2().getItemProps(itemProps), localProps);
  return createComponent(TocItemPropsProvider, {
    value: itemProps,
    get children() {
      return createComponent(ark.li, mergedProps);
    }
  });
};
var TocLink = (props) => {
  const toc2 = useTocContext();
  const itemProps = useTocItemPropsContext();
  const mergedProps = mergeProps(() => toc2().getLinkProps(itemProps), props);
  return createComponent(ark.a, mergedProps);
};
var TocList = (props) => {
  const toc2 = useTocContext();
  const mergedProps = mergeProps(() => toc2().getListProps(), props);
  return createComponent(ark.ul, mergedProps);
};
var TocNav = (props) => {
  const {
    placement,
    ...rest
  } = props;
  const toc2 = useTocContext();
  const mergedProps = mergeProps(() => toc2().getRootProps(), rest);
  return createComponent(ark.nav, mergeProps$1(mergedProps, {
    "data-placement": placement
  }));
};
var useToc = (props) => {
  const id = createUniqueId();
  const locale = useLocaleContext();
  const environment = useEnvironmentContext();
  const machineProps = createMemo(
    () => ({
      id: props?.id ?? id,
      dir: locale().dir,
      getRootNode: environment().getRootNode,
      items: props?.items ?? [],
      ...props
    })
  );
  const service = useMachine(toc.machine, machineProps);
  return createMemo(() => toc.connect(service, normalizeProps));
};

// src/components/toc/toc-root.tsx
var splitRootProps = createSplitProps();
var TocRoot = (props) => {
  const [useTocProps, localProps] = splitRootProps(props, ["activeIds", "autoScroll", "defaultActiveIds", "scrollEl", "id", "ids", "items", "onActiveChange", "rootMargin", "scrollBehavior", "threshold"]);
  const toc2 = useToc(useTocProps);
  const mergedProps = mergeProps(() => toc2().getRootProps(), localProps);
  return createComponent(TocProvider, {
    value: toc2,
    get children() {
      return createComponent(ark.div, mergedProps);
    }
  });
};
var TocRootProvider = (props) => {
  const [{
    value: toc2
  }, localProps] = createSplitProps()(props, ["value"]);
  const mergedProps = mergeProps(() => toc2().getRootProps(), localProps);
  return createComponent(TocProvider, {
    value: toc2,
    get children() {
      return createComponent(ark.div, mergedProps);
    }
  });
};
var TocTitle = (props) => {
  const toc2 = useTocContext();
  const mergedProps = mergeProps(() => toc2().getTitleProps(), props);
  return createComponent(ark.h2, mergedProps);
};

// src/components/toc/toc.ts
var toc_exports = {};
__export(toc_exports, {
  Content: () => TocContent,
  Context: () => TocContext,
  Indicator: () => TocIndicator,
  Item: () => TocItem,
  Link: () => TocLink,
  List: () => TocList,
  Nav: () => TocNav,
  Root: () => TocRoot,
  RootProvider: () => TocRootProvider,
  Title: () => TocTitle
});

export { TocContent, TocContext, TocIndicator, TocItem, TocLink, TocList, TocNav, TocRoot, TocRootProvider, TocTitle, toc_exports, useToc, useTocContext };
